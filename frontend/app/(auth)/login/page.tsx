import LoginClient from "./LoginClient";

export const metadata = {
  title: 'Login Portal Layanan Digital',
  description: 'Masuk ke portal layanan digital untuk mengelola profil dan pengajuan layanan.',
};

export default function LoginPage() {
  return <LoginClient />;
}

